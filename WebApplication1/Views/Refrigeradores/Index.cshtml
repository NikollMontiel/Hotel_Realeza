@model IEnumerable<WebApplication1.Models.Refrigeradores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EstadoR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Habitaciones.ID_Hab)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Marca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Habitaciones.ID_Hab)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Refrigerador }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Refrigerador }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Refrigerador })
        </td>
    </tr>
}

</table>
